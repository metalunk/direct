#include "RectangleSet.h"

namespace direct {
    RectangleSet::RectangleSet(
        std::vector<direct::Rectangle>& rectangles
    ) : rectangles(rectangles) {}

    void RectangleSet::calc_potentially_optimums() {
    }
}
